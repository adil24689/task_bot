# Telegram Bot Library
python-telegram-bot==20.6

# Aiogram v3 with async support
aiogram==3.6.0

# HTTP client used internally or optionally
aiohttp==3.9.5

# Pydantic with safe versions to avoid Rust build
pydantic==2.6.4
pydantic-core==2.16.3

# Optional: HTTP requests (if needed)
httpx==0.25.2

# Optional: Async utilities (aiogram/httpx depend on it)
anyio==4.2.0
sniffio==1.3.1

# Optional: Useful for type validation
annotated-types==0.7.0
